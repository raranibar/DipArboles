<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="Arboles.Views.MainPage"
             Title="Principal">
  <Grid HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
    <Grid.RowDefinitions>
      <RowDefinition Height="*" />
      <RowDefinition Height="*" />      
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <Button Command="{Binding NavComListaArbol}" Grid.Row="0" Grid.Column="0" Text="Lista Arboles" TextColor="White" BackgroundColor="#64C2F0" FontAttributes="Bold" FontSize="15"
             BorderRadius="20" HeightRequest="70" WidthRequest="170"/>
    <Button Command="{Binding NavComRegistraArbol}" Grid.Row="1" Grid.Column="0"  Text="Registro Arbol" TextColor="White" BackgroundColor="#64C2F0" FontAttributes="Bold" FontSize="15"
            BorderRadius="20" HeightRequest="70" WidthRequest="170"/>
    
  </Grid>
</ContentPage>